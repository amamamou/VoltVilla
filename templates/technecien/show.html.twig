{% extends 'base.html.twig' %}

{% block title %}Technecien{% endblock %}

{% block body %}
    <h1>Technecien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technecien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ technecien.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ technecien.Prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_technecien_index') }}">back to list</a>

    <a href="{{ path('app_technecien_edit', {'id': technecien.id}) }}">edit</a>

    {{ include('technecien/_delete_form.html.twig') }}
{% endblock %}
